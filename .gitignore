# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# project
.env
# results/ is committed: it is the evidence behind the README numbers.
results/load_test_records.json
*.log
.ruff_cache/
.mypy_cache/
.pytest_cache/
coverage.xml
.coverage
.scratch/

# build artefacts
dist/
results/bedrock/load_test_records.json

# Local tool settings and session files, never committed
.claude/
.claude.json
CLAUDE.local.md

# A live check keeps its proxy log only when it fails, for debugging
results/live/*.log

# local agent and editor files, never committed
CLAUDE.md
AGENTS.md
.DS_Store
